version: '2.3'

services:
  web:
    container_name: chitchat-qa
    restart: always
    build: .
    runtime: nvidia
    environment:
      - LANG=en_US.UTF-8
    ports:
      - "3000:3000" #web-client
